name: "eeg_warehouse"
version: "1.0.0"
config-version: 2

profile: "eeg_warehouse"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  eeg_warehouse:
    staging:
      +materialized: view
      +schema: staging
    curated:
      +materialized: table
      +schema: curated
